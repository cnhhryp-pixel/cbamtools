"use client";import {useState} from "react";
import {cbamCodeRules,cbamExcludedCodes} from "../../data/cbamCodes";
import SiteFooter from "../../components/SiteFooter";
export default function Page(){const [code,setCode]=useState(""),[result,setResult]=useState<any>(null);function check(){const q=code.replace(/\D/g,"").slice(0,10);if(q.length<4){setResult({status:"invalid"});return}if(cbamExcludedCodes.some(x=>q.startsWith(x))){setResult({status:"excluded",code:q});return}const matches=cbamCodeRules.filter(x=>q.startsWith(x.prefix)||x.prefix.startsWith(q)).sort((a,b)=>b.prefix.length-a.prefix.length);if(!matches.length){setResult({status:"unknown",code:q});return}const m=matches[0];const exactEnough=q.length>=m.prefix.length;setResult({...m,code:q,status:exactEnough?m.status:"detail"})}
return <><header className="inner-header"><a className="brand" href="/"><span className="brand-mark">C</span><span>CBAM<span className="brand-accent">Tools</span></span></a><nav><a href="/cbam-calculator">Calculator</a><a href="/hs-code-checker">HS Code Checker</a><a href="/cbam-hs-code-list">CN Codes</a><a href="/cbam-guide">Guides</a></nav><a className="header-cta" href="/cbam-calculator">Open Calculator →</a></header><main className="checker-page"><section className="checker-hero"><span className="kicker">EU CBAM PRODUCT CLASSIFICATION</span><h1>CBAM CN / HS Code Checker</h1><p>Enter a 4, 6 or 8-digit product code to screen whether goods may fall within EU CBAM scope. For a definitive classification, use the full EU CN code.</p><div className="checker-search"><input value={code} onChange={e=>setCode(e.target.value)} onKeyDown={e=>e.key==="Enter"&&check()} placeholder="Enter CN / HS code — e.g. 7318 15 90"/><button onClick={check}>Check CBAM coverage →</button></div><small>Spaces and punctuation are ignored. This screening tool is based on CBAM product-code rules and should not replace customs classification.</small></section><section className="checker-body"><div className="checker-result">{!result?<div className="checker-empty"><span>01</span><h2>Enter a product code above</h2><p>We will show the potential CBAM sector, product description and greenhouse gases where the code can be matched.</p></div>:result.status==="invalid"?<Status type="detail" title="Enter at least 4 digits" text="Use a 4, 6 or preferably 8-digit CN / HS code."/>:result.status==="unknown"?<Status type="unknown" title="Coverage not determined" text="This code was not matched by the current screening dataset. Do not interpret this as ‘not covered’. Verify the full 8-digit EU CN code against the current CBAM Annex I."/>:result.status==="excluded"?<Status type="excluded" title="Excluded by the current Annex I rule" text="The entered code matches an exclusion represented in this screening dataset. Confirm the exact 8-digit CN classification before relying on the result."/>:<><Status type={result.status} title={result.status==="covered"?"Potential CBAM coverage: COVERED":"More detail required"} text={result.status==="covered"?"The entered code matches a CBAM product heading represented in the current screening dataset.":"The code is within a CBAM-related heading, but a more specific CN/TARIC classification or an exclusion may determine the final treatment."}/><div className="result-facts"><div><span>CODE ENTERED</span><b>{result.code}</b></div><div><span>SECTOR</span><b>{result.sector}</b></div><div><span>PRODUCT</span><b>{result.product}</b></div><div><span>GREENHOUSE GAS</span><b>{result.gas}</b></div></div>{result.status==="covered"&&<div className="checker-next"><small>NEXT STEP · 02 MODEL</small><a className="checker-calc" href={"/cbam-calculator?sector="+encodeURIComponent(result.sector)+"&code="+encodeURIComponent(result.code)}>Carry this result into the calculator →</a><span>Your sector and code will be passed into the next step.</span></div>}</>}</div><aside className="checker-side"><span className="kicker">HOW TO USE</span><ol><li><b>Start with HS</b><p>Use 4 or 6 digits for an initial sector screen.</p></li><li><b>Confirm EU CN</b><p>Use the full 8-digit CN code for a more precise coverage check.</p></li><li><b>Calculate exposure</b><p>If covered, continue to the CBAM cost calculator.</p></li></ol><a href="/cbam-hs-code-list">Browse CBAM code sectors →</a></aside></section><section className="checker-sectors"><span className="kicker">CBAM SECTORS</span><h2>Browse the six CBAM sectors</h2><div><a href="/cbam-steel-products">Iron & Steel</a><a href="/cbam-aluminium-products">Aluminium</a><a href="/cbam-cement-products">Cement</a><a href="/cbam-fertilizer-products">Fertilisers</a><a href="/cbam-electricity">Electricity</a><a href="/cbam-hydrogen-products">Hydrogen</a></div></section></main><SiteFooter/></>;}
function Status({type,title,text}:{type:string,title:string,text:string}){return <div className={"coverage-status "+type}><span>{type==="covered"?"✓":type==="excluded"?"—":"!"}</span><div><small>{type==="covered"?"SCREENING RESULT":type==="excluded"?"EXCLUSION MATCH":"CHECK REQUIRED"}</small><h2>{title}</h2><p>{text}</p></div></div>;}